Object Oriented Programming (OOP)
অবজেক্ট-ওরিজেজেড প্রোগ্রোর িং (OOP) হল একটি প্রোগ্রোম িং প্কৌশল প্েখোনে সবমকছুঅবজেক্ট (Object)
আকোনে মবনবমিত হয়। অর্ থোৎ, প্কোনেোমকছুবুঝনত বো প্রোগ্রো কেনত হনল প্সিোনক ক্লোস (Class) এবিং অবজেক্ট
(Object) মহনসনব সোজোনেো হয়।
1. OOP প্েন রজেোেন?
বড় সফিওয়যোে বো অযোপ ততমে কেনত হনল প্কোডনক সহজভোনব সিংগটিত কেো দেকোে। OOP আ োনদে
প্েোড পুনঃবযবহোিজ োগ্য (Reusable) ও সহজে বযবস্থোপনোজ োগ্য (Manageable) কেনত সোহোেয
কনে।
🎯 2. OOP েী এবিং এটি রেভোজব েোে েজি....?
OOP হজ ো এ ন এেটি প্রোগ্রোর িং প্েৌশ প্ খোজন বোস্তব েীবজনি বস্তুগুজ োি (Objects)
জ ো েজি প্েোড প্ খো হে।
রমতটি অবজেজক্টি মেজস্ব তবমশষ্ট্য (Properties) এবিং আিেণ (Methods) র্োনক।
উদোহিণ: েমদ আ েো "গোমড় (Car)" মেনয় মিন্তো কমে, তোহনল
• ববরশষ্ট্য (Properties): েঙ, ব্র্যোন্ড, নডল ইতযোমদ।
• আচিণ (Methods): িোলোনেো, র্ো োনেো, গমত বোড়োনেো ইতযোমদ।
একটি ক্লোস (Class) হনলো অবনজক্ট ততমেে েকশো বো ব্লুমরন্ট, আে অবজেক্ট (Object) হনলো প্সই
ক্লোনসে বোস্তব রূপ।
3. OOP-এি ৪টি ূ ববরশষ্ট্য-
❖ Encapsulation
❖ Inheritance
❖ Polymorphism
❖ Abstraction


1️. Encapsulation:

📌 Encapsulation হলো Object-Oriented Programming (OOP)-এর একটি মূল ধারণা যেখানে ডেটা (variables) এবং তার সাথে কাজ করা function/method-গুলোকে একসঙ্গে একটি class-এর ভিতরে “bundle” করা হয়, আর বাইরের কোড থেকে ডেটাকে সরাসরি access/modify করা রোধ করা হয়। এটা ডেটা নিরাপদ রাখে এবং কোডকে আরও organized করে। 
🧑‍💻 JavaScript-এ Encapsulation উদাহরণ (সহজ)
class Person {
  #name;  // private property

  constructor(name) {
    this.#name = name;  
  }

  getName() {
    return this.#name;  // আউটসাইড থেকে name access করার public method
  }
}

const p = new Person("Rana");
console.log(p.getName());  // Output: Rana
console.log(p.#name);      // Error! direct access blocked


👉 এখানে #name হলো private variable — class-এর বাইরে থেকে সরাসরি access করা যায় না।
📌 শুধু getName() public method দিয়েই name পাওয়া যায়।


Mohi-example
//2. Encapsulation
class Counter {
  #count = 0;  // private field using #

  increment() {
    this.#count++;
  }

  getCount() {
    return this.#count;
  }
}

const counter = new Counter();
counter.increment();
console.log(counter.getCount()); // 1
// console.log(counter.#count); //  Error: Private field




🧠 Inheritance কি?

Inheritance (ইনহেরিট্যান্স) হলো OOP-এর একটা ধারণা যেখানে একটি child/derived class অন্য একটি parent/base class থেকে properties (variables) ও methods (functions) পায় এবং ঠিক যেন আগে থেকে লেখা কোড আবার লিখতে হয় না। এতে code reuse হয় এবং class-গুলোর মধ্যে একটি হায়ারার্কি তৈরি হয়। 
Wikipedia

🎯 সহজ ভাবে:
👉 Parent class যেটা — Child class সেটা inherit করে।
👉 Child class parent-এর method বা variable ডাইরেক্ট ব্যবহার করতে পারে।
👉 Child class চাইলে parent-এর method পরিবর্তন বা নতুন method যোগও করতে পারে।



//3. Inheritance
class Animal {
  constructor(name) {
    this.name = name;
  }

  speak() {
    console.log(`${this.name} makes a sound.`);
  }
}

class Dog extends Animal {
  speak() {
    console.log(`${this.name} barks.`);
  }
}

const dog = new Dog("Buddy");
dog.speak();  // Buddy barks.