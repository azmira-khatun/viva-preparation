💡 জাভাস্ক্রিপ্ট কী? (What is JavaScript?)
জাভাস্ক্রিপ্ট (JavaScript), সংক্ষেপে JS, হলো একটি হাই-লেভেল, ডাইনামিক, এবং ইন্টারপ্রেটেড (বা জাস্ট-ইন-টাইম কম্পাইলড) প্রোগ্রামিং ভাষা, যা মূলত ওয়েব ডেভেলপমেন্টের জন্য তৈরি করা হয়েছিল।

🔹 Re-declare মানে কী?

একই নামের ভ্যারিয়েবল আবার নতুন করে ঘোষণা করা

👉 মানে: আবার var / let / const লিখে সেই নাম ব্যবহার করা

উদাহরণ (Re-declare)
var x = 10;
var x = 20;   // এটাকে বলে re-declare


🔹 এখানে x নামের ভ্যারিয়েবল দুইবার ঘোষণা করা হয়েছে

🔹 Re-assign মানে কী?

আগে ঘোষিত ভ্যারিয়েবলে নতুন মান বসানো

👉 মানে: আবার নাম ব্যবহার করে মান পরিবর্তন করা
👉 var / let / const লেখা লাগে না

উদাহরণ (Re-assign)
var y = 10;
y = 30;   // এটাকে বলে re-assign


🔹 এখানে y আগেই ছিল, শুধু মান বদলানো হয়েছে

🔥 খুব সহজ পার্থক্য (মনে রাখার ট্রিক)
বিষয়	মানে
Re-declare	আবার ভ্যারিয়েবল বানানো
Re-assign	আগের ভ্যারিয়েবলের মান বদলানো

🔹 var / let / const দিয়ে পার্থক্য
var
var a = 10;
var a = 20; // re-declare ✅
a = 30;     // re-assign ✅

let
let b = 10;
// let b = 20; ❌ re-declare না
b = 20;     // re-assign ✅

const
const c = 10;
// c = 20;  ❌ re-assign না
// const c = 30; ❌ re-declare না




🔹 ফাংশন কী?

Function (ফাংশন) হলো
👉 একটা নির্দিষ্ট কাজ করার কোডের প্যাকেট,
যেটাকে বারবার ব্যবহার করা যায়।

সহজ ভাষায়:

ফাংশন = কাজের মেশিন
ডাকলে কাজ করে, না ডাকলে চুপ থাকে 🙂


🔹 JavaScript ফাংশনের উদাহরণ
1️⃣ সাধারণ ফাংশন
function greet() {
  console.log("Hello World");
}

greet(); // ফাংশন কল করা


🔹 Output:

Hello World



